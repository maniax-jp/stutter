# Stutter ユーザーマニュアル

音を刻む、止める、飛ばす、壊す。Stutter は12種類のエフェクトを小節の上に「置いて」
使う、グリッチ・エフェクトプラグインです。

macOS 専用。VST3 / AU / スタンドアロンで動作します。

---

## 目次

1. [5分で音を出す](#5分で音を出す)
2. [画面の見かた](#画面の見かた)
3. [ブロックを描く](#ブロックを描く)
4. [シーンとオートメーション](#シーンとオートメーション)
5. [エフェクト12種](#エフェクト12種)
6. [カーブで動かす](#カーブで動かす)
7. [プリセット](#プリセット)
8. [困ったときは](#困ったときは)

インストール方法は [README](../README.md#インストール) を参照してください。

---

## 5分で音を出す

**Stutter は「かける」より「置く」プラグインです。** つまみを回す前に、まず出来合いの
プリセットを鳴らしてみるのが近道です。

### 手順

1. ループ素材(ドラムループが分かりやすい)のトラックに Stutter を挿します
2. 再生します。この時点では音は変わりません
3. 画面上部の名前が書かれた欄をクリックします

![プリセットブラウザ](images/preset-browser.png)

4. **Scenes** の中から `Playable Set` を選びます
5. 音が刻まれ始めます

これで完了です。画面中央のマス目に色のついた四角が並び、上を光の線が左から右へ走って
いるはずです。**その四角が「ここでエフェクトをかける」という指示**で、光の線が現在の
再生位置です。

### 次にやること

`Playable Set` には**効きの強さ順に並んだ5つのシーン**が入っています。画面上部の
スロット一覧をクリックすると切り替わり、刻まれかたが変わります。

そして重要なのがその左の **ACTIVE** ボタンです。押すとエフェクトが切れ、原音がそのまま
通ります。もう一度押すと戻ります。

**グリッチは曲の全部にかけるものではありません。** 「どこで効かせるか」を決めるのが
ACTIVE で、それを DAW のオートメーションに書くのが本来の使い方です。次章で説明します。

---

## 画面の見かた

![全体画面](images/overview.png)

上から順に5つの区画に分かれています。

### ① ヘッダー(いちばん上)

| 表示 | 意味 |
|---|---|
| `<` `名前` `>` | プリセットの選択。矢印で前後に送れます |
| `Save` | 現在の状態に名前を付けて保存 |
| `DRY/WET` | 原音とエフェクト音の混ぜ具合。左に回すほど原音だけ |
| `OUTPUT` | 出力音量 |
| `SEQ` | エフェクトの ON / OFF。切ると原音がそのまま出ます |
| `SYNC` | DAW のテンポに合わせるかどうか |
| `120.0 BPM` | 現在のテンポ |

プリセット名の右に `*` が付いていたら「保存していない変更がある」という意味です。

### ② シーンストリップ(横並びのマス)

![シーンとブロック](images/lane-params.png)

マスひとつひとつが「シーン」です。**すべてのマスに番号が振ってあり**、これがそのまま
オートメーションで打ち込む番号になります。数え直す必要はありません。

枠の意味が2種類あります。

| 枠 | 意味 |
|---|---|
| **水色の枠 + 外側の光** | 今**鳴っている**シーン。オートメーションが動かすと自動で移動します |
| **白い枠** | 今**編集している**シーン。クリックしたときだけ動きます |

分かれているのは、**再生しながら別のシーンを編集できる**ようにするためです。

**色が付いているマスには中身が入っています。**

### ③ 演奏バー(細い帯)

`ACTIVE` `GRID` `SWING`。ACTIVE はエフェクトを効かせるかどうか、GRID と SWING は
1小節の刻みかたを決めます。

### ④ ブロックグリッド(まんなかの広いマス目)

左端に12個のエフェクト名が縦に並んでいます。これが **レーン** です。
横方向は時間で、1小節分が表示されています。

色付きの四角が **ブロック** です。ブロックが置かれている区間だけ、そのレーンの
エフェクトがかかります。

太い縦線は拍の区切り、細い線はその中の刻みです。光って動いている縦線が再生位置です。

### ⑤ 下部タブ

| タブ | 中身 |
|---|---|
| `LANE: ○○` | 選んでいるレーンのつまみ |
| `VOLUME` `FILTER` `PAN` | 音量・フィルター・定位を絵で描いて動かす |
| `MOD` | カーブを好きなつまみに繋ぐ設定 |

レーン名(左端)をクリックすると、そのレーンが選ばれてタブの中身が変わります。

---

## ブロックを描く

ブロックの操作はマウスだけで完結します(ソロだけ Option キーを使います)。

| やりたいこと | 操作 |
|---|---|
| 置く | 何もない所を**ドラッグ**。伸ばした分だけの長さになります |
| 動かす | ブロックの**まんなかをドラッグ** |
| 長さを変える | ブロックの**左端か右端をドラッグ** |
| 消す | ブロックを**右クリック** |
| まとめて消す | **右ドラッグ**でなぞる |
| レーンを全消去 | レーン上で**右ダブルクリック** |

置いた瞬間から音に反映されます。ひとつのドラッグは「1回の操作」として扱われるので、
`⌘Z` で元に戻せます。

### レーンを一時的に黙らせる

レーン名の左にある**小さな点**がミュートボタンです。

| 操作 | 動作 |
|---|---|
| 点をクリック | そのレーンをミュート(点が中抜きになります) |
| 点を **Option+クリック** | そのレーンだけを鳴らす(ソロ。点に輪が付きます) |

黙っているレーンのブロックは**灰色に沈んで**表示されるので、「なぜ鳴らないのか」が
グリッドを見れば分かります。ミュートは**シーンごとに保存されます。**

### 長さが音を変えます

**これが Stutter でいちばん大事な考えかたです。**

たとえば `TAPE STOP`(テープの停止音)を1マスだけ置くと、減速が始まった途端に次の
マスが来て、ほとんど何も起きません。同じブロックを**1小節まるごとに伸ばす**と、
1小節かけてゆっくり停止する、あの効果になります。

`Held Envelopes` プリセットがこれを実演しています。TAPE STOP のブロックが横一杯に
伸びているのが見えるはずです。

短く置く効果と長く置く効果は、**同じエフェクトでもまったく別物**だと考えてください。

---

## シーンとオートメーション

### シーンとは

**画面の状態まるごとのスナップショット**です。ブロックの配置、12レーンすべてのつまみ、
カーブ、グリッドの刻みかた、スイングまで、全部が1つのシーンに入っています。

シーンは127個持てます。番号は **1 〜 127** で、画面上部のマスに振ってある番号と、DAW の
オートメーションで打ち込む番号は**まったく同じもの**です。画面で「3」と見えているシーンを
鳴らしたければ、オートメーションに 3 と書きます。

### 基本の考えかた

Stutter の使いかたは2段階に分かれています。

| どこで | 何をする |
|---|---|
| **プラグイン画面** | エフェクトを組む(ブロックを置き、つまみを決め、カーブを描く) |
| **DAW のオートメーション** | 組んだものを**どこで**使うか決める |

グリッチは曲のほとんどの小節では鳴っていないほうが自然です。サビ前の1小節だけ、
ブレイクの2拍だけ、というのが実際の使いかたでしょう。**その「どこで」を決めるのが
オートメーションの仕事**です。

### オートメーションで使う2つの値

DAW のオートメーションレーンに、Stutter のパラメータとして次の2つが出てきます。

| 名前 | 範囲 | 意味 |
|---|---|---|
| **Scene** | 1 〜 127 | どのシーンを鳴らすか。画面のマスの番号と同じ |
| **Active** | オン / オフ | そこで効かせるか |

`Scene` に **0** を指定すると「指定なし」の扱いになり、シーンは切り替わりません。
オートメーションを書いていないレーンは 0 として読まれるので、**うっかりシーン1に
飛ばされることがない**ようになっています。

**この2つだけです。** レーンのつまみ(Filter の Cutoff など)は個別にオートメーション
できません。つまみは画面で決めてシーンに保存し、オートメーションでは「どのシーンか」を
選ぶ、という分担になっています。

### 手順

1. プラグイン画面でシーンを組みます(番号を覚えておきます)
2. DAW でトラックのオートメーションレーンを開きます
3. **Stutter → Active** を選びます
4. グリッチを効かせたい小節だけオンにします
5. 複数のシーンを使い分けるなら、**Stutter → Scene** のレーンも開いて番号を書きます

再生すると、プラグイン画面の ACTIVE ボタンとシーンストリップの水色枠が
**オートメーションに合わせて動きます**。画面を開いたまま再生すれば、今どうなっているかが
そのまま見えます。

### オートメーションを書かない場合

画面の ACTIVE ボタンとシーンのクリックが、そのまま効きます。オートメーションは
「書いてあれば優先される」のではなく、**書けばそれが値を動かす**という関係です。
どちらか一方を選ぶ必要はありません。

### Active をオフにすると何が起きるか

**原音がそのまま通ります。無音にはなりません。**

これは意図的な設計です。エフェクトが切れた瞬間にトラックに穴が開くと使いものに
ならないので、切れているときは素材がそのまま流れます。

切り替わりは約5ミリ秒かけて滑らかに行われるため、**プチッというノイズは出ません**。

### 空のシーンを指定したら

何も起きません。**音が消えるわけではなく、原音がそのまま通ります。**

中身のあるシーンには色が付いているので、画面上部のマスを見れば分かります。

### 演奏バー

シーンストリップのすぐ下の細い帯です。

![演奏バー](images/performance-bar.png)

| 表示 | 何を決めるか |
|---|---|
| **ACTIVE** | エフェクトを効かせるか。オートメーションで動かせます |
| **GRID** | 1小節をどう刻むか(拍数 × 分割数)。シーンごと |
| **SWING** | 偶数拍を後ろにずらしてハネさせる。シーンごと |

#### ACTIVE

点灯していれば効いている、暗ければ切れている、という表示です。クリックで切り替わり、
オートメーションでも動きます。**再生中はオートメーションが動かすので、押すのではなく
読むものになります。**

#### GRID(拍数 × 分割数)

たとえば `4 × 4` なら16分刻み、`4 × 3` なら三連、`3 × 4` なら3拍子です。

**変えてもブロックは動きません。** ブロックは「何分割目にあるか」で覚えているので、
グリッドを変えると同じ配置が新しい刻みで鳴り直します。

#### SWING

`0.00` で均等、プラスでハネます。マイナスにすると逆に前ノリになります。

**小節の長さは変わりません。** 奇数番目の境界だけがずれ、偶数番目は固定されます。

---

## エフェクト12種

上から順に並んでいます。同じ列(縦方向)に複数のブロックを置くと、重ねがけになります。

### 音を切り貼りするもの

| レーン | 何が起きるか |
|---|---|
| **STUTTER** | 短く切り取って繰り返す。いちばん基本のグリッチ |
| **TAPE STOP** | テープが止まるように減速する。**長く置くほど効きます** |
| **TAPE START** | 逆に、止まった状態から再生速度まで加速する |
| **REVERSE** | 逆再生する |
| **REPITCH** | 音程を変える |
| **STRETCHER** | 引き伸ばす。細かい粒に分けて散らします |
| **SHUFFLER** | 細切れにして順番を入れ替える。確率で動くので毎回少し違います |

### 音色を変えるもの

| レーン | 何が起きるか |
|---|---|
| **GATE** | 音を細かく途切れさせる。トランスゲート |
| **FILTER** | 高域や低域を削る |
| **CRUSH** | ビットを荒くして汚す。ローファイ化 |
| **DISTORT** | 歪ませる。4種類の歪みかたを選べます |
| **DELAY** | やまびこ。他と違い、原音とは別系統で返ってきます |

### つまみの見かた

![レーンつまみ](images/lane-params.png)

レーン名をクリックすると、下の `LANE:` タブにそのレーンのつまみが出ます。
つまみの下の数字は直接入力もできます。

上の画面は TAPE STOP を選んだところで、`CURVE`(減速のかかりかた)と
`TIME`(減速にかける時間)の2つがあります。

---

## カーブで動かす

`VOLUME` `FILTER` `PAN` の3つのタブでは、**線を描いて**音量・音色・定位を
時間変化させられます。

![カーブ](images/tab-volume-duck.png)

上の例は `Duck` を押したところです。頭で音量がガクッと落ちて戻ってくる、
サイドチェイン風のポンプ効果になります。

### 操作

| やりたいこと | 操作 |
|---|---|
| 形を選ぶ | `Saw Dn` `Saw Up` `Sine` `Square` `Duck` `Steps` のボタン |
| 点を動かす | 点を**ドラッグ** |
| 点を足す | 線の何もない所を**ダブルクリック** |
| 点を消す | 点を**ダブルクリック**(両端は消せません) |
| 線をふくらませる | 線の上で**右ドラッグ**(上下で曲がりかたが変わる) |
| 繰り返す速さ | 右端のメニュー(`1/16` など) |
| ON / OFF | 左端の `ON` ボタン |

### MOD タブ ― カーブを他のつまみに繋ぐ

![MODタブ](images/tab-mod-routes.png)

`VOLUME` `FILTER` `PAN` は決まった行き先に繋がっていますが、**MOD タブを使うと
どのレーンのどのつまみでもカーブで動かせます。**

`+ ROUTE` を押すと1行増えます。

| 欄 | 意味 |
|---|---|
| 左のメニュー | 動かしたいつまみ(例: `Filter Cutoff`) |
| スライダーと数値 | どれくらい動かすか(0〜1) |
| `1x` | カーブが1小節に何周するか |
| `X` | この行を削除 |

上の画面は `Routed Modulation` プリセットで、フィルターのカットオフをカーブで
動かしている状態です。

---

## プリセット

### 種類

| カテゴリ | 中身 |
|---|---|
| **Init** | まっさら。ここから作り始めます |
| **Scenes** | オートメーションで切り替える、複数シーン入りのセット(4種) |
| その他 | 1画面ぶんの単体プリセット(28種) |

**Scenes カテゴリの4つ**は、それぞれ違う特徴を見せるために作られています。

| 名前 | 何を見せているか |
|---|---|
| `Held Envelopes` | 長いブロックで TAPE STOP がきちんと停止しきる例 |
| `Swing & Odd Grids` | 3連や5拍子など、変わった刻みかた |
| `Routed Modulation` | カーブでつまみを動かす例 |
| `Playable Set` | C4〜G4 に効きの強さ順で配置した演奏用 |

### 保存

`Save` を押して名前を付けると保存されます。保存先はここです。

```
~/Library/Audio/Presets/Maniax/Stutter/
```

書き出されるのは XML ファイルなので、他の人に渡すこともできます。

---

## 困ったときは

### 音が変わらない

- `SEQ` が OFF になっていませんか(ヘッダー右側)
- `DRY/WET` が左に振り切れていませんか
- **`ACTIVE` が消えていませんか。** 暗く沈んでいたら効いていません。クリックするか、
  オートメーションで ON になっているか確認してください
- **ブロックがひとつも置かれていないシーンを見ていませんか。** 画面上部のマスで色の
  付いたものを選んでください
- 再生していますか。Stutter は DAW の再生位置に合わせて動きます

### プツプツ、パチパチいう

エフェクトの切り替わり自体にノイズは出ない設計です。以下を確認してください。

- `CRUSH` や `DISTORT` を強くかけていませんか。これらは**意図的に歪ませる**ものです
- `OUTPUT` を上げすぎて音が割れていませんか

### 短いブロックで TAPE STOP が効かない

仕様です。[ブロックを描く](#長さが音を変えます)を参照してください。ブロックを
長くしてください。

### オートメーションでシーンが切り替わらない

- DAW のレーンで **Stutter → Scene** を選んでいますか(`Active` とは別のレーンです)
- 指定した番号のシーンに中身がありますか(画面上部のマスで色が付いているか)
- 画面上部の水色枠が動いていれば切り替わっています。音が変わらないなら、そのシーンが
  空か、`ACTIVE` が OFF です

### 同じ設定なのに毎回少し違う音になる

`SHUFFLER` と `STRETCHER` は確率で動くレーンです。ただし**同じ小節では毎回同じ結果**に
なるよう作られているので、書き出した音と聴いていた音が食い違うことはありません。

---

## 現在の制限

- **v1(1.1.2)で保存したプロジェクトの設定は読み込めません。** 開くと初期状態に
  なります。v1 のプリセット28種は v2 用に作り直して同梱してあります
- **MIDI には対応していません。** シーンの切り替えと ON/OFF は、DAW の
  オートメーションで行います。鍵盤で演奏する機能はありません
- **レーンのつまみは個別にオートメーションできません。** つまみは画面で決めてシーンに
  保存し、オートメーションでは「どのシーンか」を選ぶ設計です
- **シーンストリップは24枠までしか表示されません。** シーンは127個持てますが、
  1番から24番までが見えています
- **オーバーサンプリングは行っていません。** `CRUSH` `DISTORT` `REPITCH` を高い設定に
  すると、ざらついた倍音が乗ります。グリッチ用途としては味になりますが、
  きれいな音程変化を求める用途には向きません
- **DAW のプログラム切り替え機能には未対応です。** プリセットは Stutter 自身の
  ブラウザから選んでください
