# Changelog

## v2.0.0

Stutter が「常時かけるエフェクト」から「**MIDI で演奏する楽器**」になりました。

### ⚠️ 破壊的変更

**v1 で保存したプロジェクトの状態は読み込まれません。** 開くと Init 状態になります。

v1 は8レーン×16ステップの固定グリッドで、v2 のシーンモデルにはそれを表現する形が
ありません。中途半端に読み込んで別物の音を出すより、明示的に拒否する判断です。

- **v1 のファクトリープリセット28個は変換して同梱**しています。音は失われません
- v1 で作った**ユーザープリセットは読み込めません**。必要な設定は v2 で作り直してください
- v1.1.2 を使い続ける場合は、そちらのリリースをそのままお使いください

### 新機能

**Scene — MIDI ノートで切り替える128の状態**

ブロック配置・全パラメータ・カーブを丸ごと記憶したスナップショットを、鍵盤に割り当てて
演奏できます。サビの入りで C4、ブレイクで E4 といった使いかたができます。切り替えは
音を切らずに行われます。

**可変長ブロックのシーケンサー(12レーン)**

固定16ステップをやめ、ブロックが**開始位置と長さ**を持つようになりました。これが
v2 で最も音に効く変更です。v1 では16分ごとにエフェクトが再スタートしていたため、
TapeStop の減速が**停止に到達できませんでした**。ブロックを1小節に伸ばせば、1小節
かけて止まりきります。

- Beats 1〜8 × Divisions 2〜8 と Swing。3や6を選べば三連、5や7で変則グリッド
- 新レーン4種: **Stretcher**(グラニュラー)/ **Shuffler**(確率的スライス並べ替え)/
  **Delay**(Send 系統。フィードバックを直列チェーンから隔離)/ **Distortion**(4種)

**ルーティング可能なモジュレーション**

v1 のカーブは Volume / Filter / Pan に固定配線でした。v2 は MOD タブで**任意のカーブを
任意のレーンパラメータへ**接続できます。

**MIDI 演奏レイヤー**

- **Play Mode** — Auto(常時)/ MIDI(押している間だけ)
- **クオンタイズ** — 少し早く弾いても、狙った区切りで発音します
- **Release モード5種**(シーンごと)— On Grid / Full / Latch / Instant / Stick
- **Scene Lock** — 打ち込み MIDI を流しながら1つのシーンを編集できます

**レーンのミュート / ソロ**

レーン名の左の点をクリックでミュート、Option+クリックでソロ。黙っているレーンは
グリッド上で灰色に沈むので、「なぜ鳴らないのか」が見て分かります。

### その他の変更

- UI が 1200×800 に拡大。シーンストリップ(鍵盤)と演奏バーを追加
- ファクトリー Scene バンク4種(14シーン)を追加
- レート表示の4倍ずれを修正。**表示だけの修正**で、プリセットの音は変わりません

### 既知の制約

- ノート → シーンの対応は固定です(C4 = シーン60)。任意割り当ての UI はありません
- Texture レーンの並べ替え UI はありません。順序はグリッドの上から下で固定です
- ループ方式は Forward のみです(Palindrome / OneShot は未実装)
- オーバーサンプリングは行っていません。Crush / Distortion / Repitch を高く設定すると
  ざらついた倍音が乗ります
- DAW のプログラム切り替え機能には未対応です

### 検証

CI(macOS 14、Apple Silicon + Intel ユニバーサル)で以下を通過しています:

- Catch2 テストスイート(54ケース / 2186アサーション)
- **ゴールデンベースライン照合** — 各レーンのレンダリング結果を v1.1.2 と SHA-256 で
  比較しています。**8レーン中7レーンは v1.1.2 とビット単位で同一**です。Filter のみ
  シーケンサー刷新時に基準を更新しましたが、差分は実測で最大1LSB / 24bit
  (≒ −138 dBFS)の浮動小数点丸め差であり、聴感上の変化ではありません
- `pluginval --strictness-level 8`(VST3)/ `auval`(AU)
- Developer ID 署名 + Apple 公証

### インストール

`Stutter-2.0.0-macOS.zip` を展開し、以下に配置してください。

| 形式 | 配置先 |
|---|---|
| VST3 | `~/Library/Audio/Plug-Ins/VST3/` |
| Audio Unit | `~/Library/Audio/Plug-Ins/Components/` |
| Standalone | 任意の場所(`/Applications` など) |

使いかたは [MANUAL.md](docs/MANUAL.md) を参照してください。

## v1.1.2 以前

[リリース一覧](https://github.com/maniax-jp/stutter/releases)を参照してください。
